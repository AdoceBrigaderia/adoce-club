import { useEffect, useMemo, useState } from "react";
import { Clock3, Heart, MapPin, MessageCircle, ShoppingBag, Sparkles } from "lucide-react";
import { isSupabaseConfigured, requireSupabase } from "./lib/supabase";
import "./adoce-hoje.css";

type Availability = "all" | "available" | "premium";
type Flavor = { id:string; name:string; note:string; price:number; image:string; available:boolean; premium:boolean; status?:string; };
type Channel = { slug:string; label:string; status:"open"|"closed"|"opening_soon"|"paused"; message:string|null; next_change_at:string|null; };

const fallback: Flavor[] = [
  {id:"kinder",name:"Kinder Bueno",note:"Chocolate cremoso com Kinder Bueno.",price:20,image:"/adoce-hoje/kinder-bueno.webp",available:false,premium:true},
  {id:"ninho",name:"Trufado de Ninho com morangos",note:"Chocolate trufado, Ninho e morangos.",price:16,image:"/adoce-hoje/chocolatudo-morangos.webp",available:false,premium:false},
  {id:"red",name:"Red Velvet com Ninho e Nutella",note:"Red Velvet, Ninho e Nutella.",price:16,image:"/adoce-hoje/red-velvet.webp",available:false,premium:false},
  {id:"choco",name:"Chocolatudo",note:"Chocolate intenso e muito recheio.",price:16,image:"/adoce-hoje/chocolatudo.webp",available:false,premium:false},
  {id:"ferrero",name:"Ferrero Rocher",note:"Chocolate, creme e crocância.",price:16,image:"/adoce-hoje/ferrero-rocher.webp",available:false,premium:false},
  {id:"oreo",name:"Oreo",note:"Chocolate, creme e Oreo.",price:16,image:"/adoce-hoje/oreo.webp",available:false,premium:false},
  {id:"limao",name:"Limão com frutas vermelhas",note:"Cítrica, cremosa e frutada.",price:16,image:"/adoce-hoje/limao-frutas-vermelhas.webp",available:false,premium:false},
  {id:"abacaxi",name:"Abacaxi com coco",note:"Tropical, cremosa e molhadinha.",price:16,image:"/adoce-hoje/abacaxi-coco.webp",available:false,premium:false},
];

const whatsappBase = "https://wa.me/5585982156026?text=";
const maps = "https://www.google.com/maps/search/?api=1&query=Festival%20de%20Fatias%20Adoce%20Brigaderia";
const orderLink = (flavor?:string) => whatsappBase + encodeURIComponent(flavor ? `Olá, Adoce! Quero saber se a fatia ${flavor} está disponível.` : "Olá, Adoce! Quero conhecer os sabores disponíveis hoje.");

function Brand(){return <a className="today-brand" href="/"><img src="/site/logo.webp" alt="Adoce Brigaderia"/><span><strong>Clube Adoce</strong><small>Adoce Hoje</small></span></a>}

export default function AdoceHoje(){
  const [filter,setFilter]=useState<Availability>("all"); const [flavors,setFlavors]=useState<Flavor[]>(fallback); const [channels,setChannels]=useState<Channel[]>([]); const [updated,setUpdated]=useState(false);
  useEffect(()=>{document.title="Adoce Hoje · Clube Adoce";},[]);
  const dateLabel = useMemo(()=>new Intl.DateTimeFormat("pt-BR",{weekday:"long",day:"2-digit",month:"long"}).format(new Date()),[]);
  useEffect(()=>{if(!isSupabaseConfigured)return;void(async()=>{const supabase=requireSupabase();const today=new Date().toISOString().slice(0,10);const [{data:catalog},{data:availability},{data:channelData}]=await Promise.all([supabase.from("flavors").select("id,name,category,short_description,description,image_path,base_price").eq("active",true).order("sort_order"),supabase.from("flavor_availability").select("flavor_id,status,note").eq("service_date",today),supabase.from("store_channels").select("slug,label,status,message,next_change_at").order("slug")]);if(catalog?.length){setFlavors(catalog.map(item=>{const status=availability?.find(a=>a.flavor_id===item.id);return{id:item.id,name:item.name,note:item.short_description||item.description||"Feita com carinho em cada camada.",price:Number(item.base_price||0),image:item.image_path||"/adoce-hoje/sabores-hoje.webp",available:Boolean(status&&["available","last_units","preorder_only"].includes(status.status)),premium:item.category==="premium",status:status?.status};}));}if(channelData)setChannels(channelData as Channel[]);setUpdated(true);})();},[]);
  const visible=useMemo(()=>flavors.filter(f=>filter==="all"||(filter==="available"?f.available:f.premium)),[flavors,filter]); const inPerson=channels.find(c=>c.slug==="in_person"); const online=channels.find(c=>c.slug==="online_orders"); const open=inPerson?.status==="open"; const availableCount=flavors.filter(f=>f.available).length;
  const statusText=(channel?:Channel)=>channel?.status==="open"?"Aberto agora":channel?.status==="opening_soon"?"Abriremos em breve":channel?.status==="paused"?"Pausado":"Fechado agora";
  return <main className="today-page"><header className="today-header"><Brand/><nav><a href="#sabores">Sabores</a><a href="#atendimento">Atendimento</a><a className="today-order-small" href={orderLink()} target="_blank" rel="noreferrer"><MessageCircle/> Consultar</a></nav></header><section className="today-hero"><div className="today-hero-copy"><div className="today-live"><span className={open?"":"closed"}/> Informações atualizadas</div><p className="today-kicker">{dateLabel}</p><h1>Descubra o que pode <em>adoçar seu dia.</em></h1><p className="today-lead">Sabores, disponibilidade e atendimento reunidos em um só lugar — sempre com uma informação honesta antes de você sair de casa.</p><div className="today-status-grid"><div><ShoppingBag/><span><strong>{availableCount?`${availableCount} sabores sinalizados`:"Consulte a disponibilidade"}</strong><small>{updated?"Catálogo conectado ao Clube Adoce":"Carregando informações"}</small></span></div><div><Clock3/><span><strong>{statusText(inPerson)}</strong><small>{inPerson?.message||"Atendimento presencial"}</small></span></div></div><div className="today-hero-actions"><a className="today-primary" href={orderLink()} target="_blank" rel="noreferrer"><MessageCircle/> Falar com a Adoce</a><a className="today-secondary" href={maps} target="_blank" rel="noreferrer"><MapPin/> Como chegar</a></div></div><figure className="today-poster"><img src="/adoce-hoje/sabores-hoje.webp" alt="Seleção de fatias Adoce"/><figcaption>Feitas para transformar vontade em felicidade</figcaption></figure></section><section className="today-flavors" id="sabores"><div className="today-section-head"><div><p className="today-kicker">Catálogo Adoce</p><h2>Escolha sua próxima paixão</h2></div><p>A disponibilidade muda ao longo do dia. Quando um sabor não estiver sinalizado, consulte pelo WhatsApp antes de pedir.</p></div><div className="today-filters"><button className={filter==="all"?"active":""} onClick={()=>setFilter("all")}>Todos <span>{flavors.length}</span></button><button className={filter==="available"?"active":""} onClick={()=>setFilter("available")}>Sinalizados hoje <span>{availableCount}</span></button><button className={filter==="premium"?"active":""} onClick={()=>setFilter("premium")}>Premium <span>{flavors.filter(f=>f.premium).length}</span></button></div><div className="today-card-grid">{visible.map(flavor=><article className="today-flavor-card" key={flavor.id}><div className="today-card-image"><img src={flavor.image} alt={`Fatia ${flavor.name}`} loading="lazy"/>{flavor.premium&&<span className="today-premium"><Sparkles/> Premium</span>}</div><div className="today-card-body"><div><div className="today-card-status">{flavor.available?<><span className="now"/> {flavor.status==="last_units"?"Últimas unidades":"Sinalizado hoje"}</>:<><Clock3/> Consulte antes de pedir</>}</div><h3>{flavor.name}</h3><p>{flavor.note}</p></div><div className="today-price"><small>a partir de</small><strong>{flavor.price?`R$ ${flavor.price.toFixed(2).replace(".",",")}`:"Consulte"}</strong></div></div><a href={orderLink(flavor.name)} target="_blank" rel="noreferrer"><MessageCircle/> Quero esta</a></article>)}</div></section><section className="today-event" id="atendimento"><div className="today-event-copy"><p className="today-kicker">Atendimento Adoce</p><h2>{open?"Estamos te esperando.":"Confira antes de sair."}</h2><p><strong>Presencial:</strong> {statusText(inPerson)}. {inPerson?.message||"Os horários serão publicados aqui."}</p><p><strong>Pedidos online:</strong> {statusText(online)}. {online?.message||"Consulte pelo WhatsApp."}</p><a className="today-primary" href={maps} target="_blank" rel="noreferrer"><MapPin/> Abrir localização</a></div><div className="today-contact"><Heart/><h3>Fale com a gente</h3><a href={orderLink()} target="_blank" rel="noreferrer">85 98215-6026</a><a href="https://wa.me/5585981994370" target="_blank" rel="noreferrer">85 98199-4370</a><small>Confirme o sabor e a forma de atendimento antes do deslocamento.</small></div></section><footer className="today-footer"><Brand/><p>Aqui na Adoce você compra a fatia e a felicidade vai junto.</p></footer><div className="today-mobile-bar"><a href={maps} target="_blank" rel="noreferrer"><MapPin/> Chegar</a><a href={orderLink()} target="_blank" rel="noreferrer"><MessageCircle/> Consultar</a></div></main>;
}
