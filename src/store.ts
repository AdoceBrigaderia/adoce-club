import { create } from 'zustand';
export type Payment='Dinheiro'|'Pix'|'Cartão'|'Cortesia'|'Fidelidade';
export type Sale={id:string;qty:number;value:number;payment:Payment;kind:'Presencial'|'Delivery / Retirada';seller:string;token:string;claimed:boolean;createdAt:string};
type State={role:'cliente'|'vendedor'|'admin';stamps:number;reward:number;cashOpen:boolean;cashFund:number;available:number;price:number;cost:number;sales:Sale[];family:{name:string;code:string;members:string[]}|null;referralCode:string;referralBonus:boolean;setRole:(r:State['role'])=>void;openCash:(fund:number,available:number)=>void;addSale:(s:Omit<Sale,'id'|'token'|'claimed'|'createdAt'>)=>Sale;claim:(token:string)=>boolean;createFamily:()=>void;joinFamily:(code:string)=>boolean;activateReferral:()=>void;setConfig:(price:number,cost:number)=>void};
const initialSales:Sale[]=[{id:'A12',qty:3,value:48,payment:'Pix',kind:'Presencial',seller:'Maria Júlia',token:'ADOCE-A12',claimed:true,createdAt:'09:35'},{id:'A11',qty:2,value:32,payment:'Dinheiro',kind:'Delivery / Retirada',seller:'Carlos Silva',token:'ADOCE-A11',claimed:false,createdAt:'09:20'}];
export const useStore=create<State>()((set,get)=>({role:'cliente',stamps:9,reward:0,cashOpen:false,cashFund:0,available:72,price:16,cost:6.67,sales:initialSales,family:null,referralCode:'BEZERRA10',referralBonus:false,setRole:role=>set({role}),openCash:(cashFund,available)=>set({cashOpen:true,cashFund,available}),addSale:data=>{const id=`A${Math.floor(100+Math.random()*900)}`;const sale={...data,id,token:`ADOCE-${id}-${Date.now().toString(36).toUpperCase()}`,claimed:false,createdAt:new Date().toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'})};set(s=>({sales:[sale,...s.sales]}));return sale},claim:token=>{const sale=get().sales.find(s=>s.token===token);if(!sale||sale.claimed)return false;const total=get().stamps+sale.qty;set(s=>({stamps:total%14,reward:s.reward+Math.floor(total/14),sales:s.sales.map(x=>x.token===token?{...x,claimed:true}:x)}));return true},createFamily:()=>set({family:{name:'Família Bezerra',code:'FAMILIA-DOCE',members:['Rubens B.']}}),joinFamily:code=>{if(code.toUpperCase()!=='FAMILIA-DOCE')return false;set({family:{name:'Família Bezerra',code:'FAMILIA-DOCE',members:['Rubens B.','Novo membro']}});return true},activateReferral:()=>{if(get().referralBonus)return;const total=get().stamps+1;set(s=>({referralBonus:true,stamps:total%14,reward:s.reward+Math.floor(total/14)}))},setConfig:(price,cost)=>set({price,cost})}));
