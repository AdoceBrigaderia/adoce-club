server { listen 80; root /usr/share/nginx/html; index index.html; location / { try_files $uri /index.html; } location = /health/live { default_type application/json; return 200 '{"status":"ok"}'; } location = /health/ready { default_type application/json; return 200 '{"status":"ready","mode":"demo"}'; } }
