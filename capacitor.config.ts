import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.adocebrigaderia.operacao',
  appName: 'Operação Adoce',
  webDir: 'dist'
};

export default config;
